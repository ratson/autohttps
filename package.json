{
  "private": true,
  "scripts": {
    "prettier": "prettier --single-quote --trailing-comma es5 --write 'packages/*/{.,examples,lib,test,tests}/*.js'"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.0",
    "prettier": "^0.22.0"
  }
}
